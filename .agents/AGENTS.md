# Moved

This file's content moved to [`/AGENTS.md`](../AGENTS.md) at the repository root,
so that agent tooling which auto-discovers `AGENTS.md` at the repo root (the
common cross-tool convention) finds it without needing a pointer from a
tool-specific file like `CLAUDE.md`.

Kept here only as a redirect for anything that still links to this path
directly. Don't re-add rule content to this file — it previously drifted out of
sync with the dedicated skill files it summarized (an inaccurate restatement of
the EDR test-execution rule lived here for some time). One canonical file per
topic, linked rather than restated, is the whole point of the root router.

See [`/AGENTS.md`](../AGENTS.md).

<!-- markdownlint-disable MD049 -->
---
*Last Updated: 2026-07-31* | *Last Reviewed: 2026-07-31*
