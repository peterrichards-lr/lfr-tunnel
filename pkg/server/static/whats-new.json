{
  "version": "v1.14.1",
  "release_date": "2026-06-23",
  "features": [
    "Implement Gateway-First dynamic self-upgrade mechanism (lfr-tunnel -upgrade) which queries the gateway's /api/version endpoint directly to bypass GitHub API rate limits.",
    "Support dynamic package manager instructions (Homebrew, Scoop, Custom Install Script) returned from the gateway instead of attempting in-place binary swaps."
  ]
}


